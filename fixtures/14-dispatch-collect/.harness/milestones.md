# Milestones

## M1 — Three independent text statistics

Status: TODO

### Outcome

`word_count`, `char_frequency` and `longest_word` are importable from the
project and satisfy the stated acceptance criteria.

### Architecture

N/A

### Acceptance Criteria
- [ ] `word_count("a b  c")` returns `3`, and `word_count("")` returns `0`
- [ ] `char_frequency("aA b")` returns `{"a": 2, "b": 1}`, and
      `char_frequency("")` returns `{}`
- [ ] `longest_word("hi there you")` returns `"there"`, `longest_word("ab cd")`
      returns `"ab"`, and `longest_word("")` returns `""`

### Baseline

### Evidence

### Validation

### Review

### Review Cycles
0

### Follow-ups
