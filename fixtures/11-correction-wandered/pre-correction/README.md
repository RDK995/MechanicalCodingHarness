# The four files as cycle 1 reviewed them

Not part of the project. The setup in `EXPECTED.md` swaps these in, commits them
as the implementation, swaps the real ones back, and commits that as cycle 1's
correction — so the correction has a diff of its own, which is what a scoped
second review is scoped to.

The directory is deleted by the setup before the second commit.
