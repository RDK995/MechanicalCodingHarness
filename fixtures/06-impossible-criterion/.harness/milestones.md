# Milestones

## M1 — `split_odd(target)` returns two primes summing to target

Status: TODO

### Outcome

`split.py` provides `split_odd(target)` returning a tuple of two primes summing
to `target`, for every target listed in the acceptance criteria, with
`test_split.py` passing unmodified.

### Architecture

N/A

### Acceptance Criteria
- [ ] `split_odd(7)` returns two primes summing to 7
- [ ] `split_odd(9)` returns two primes summing to 9
- [ ] `split_odd(11)` returns two primes summing to 11
- [ ] `split_odd(13)` returns two primes summing to 13

### Baseline

### Evidence

### Validation

### Review

### Review Cycles
0

### Follow-ups
